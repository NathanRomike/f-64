import { totalCart } from '../../../helpers/total-cart';
import { module, test } from 'qunit';

module('Unit | Helper | total cart');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = totalCart(42);
  assert.ok(result);
});
